name: githubsecuritylab/codeql-bicep-tests
groups: [bicep, test]
dependencies:
  githubsecuritylab/codeql-bicep-all: ${workspace}
  githubsecuritylab/codeql-bicep-queries: ${workspace}
extractor: bicep
tests: .
warnOnImplicitThis: true
